pipeline {
	agent none
  stages {
  	stage('Maven Install') {
    	agent {
      	docker {
        	image 'spring-boot-docker.jar'
        }
      }
      steps {
      	bat 'mvn clean install'
      }
    }
  }
}
