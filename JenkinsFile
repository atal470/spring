pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        bat 'docker build -t my-spring-boot-app:latest .'
      }
    }

    stage('Run') {
      steps {
        bat 'docker run -d -p 9000:8080 my-spring-boot-app:latest'
      }
    }
  }
}
